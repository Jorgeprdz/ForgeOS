# Forge Aura Pipeline UX Reconciliation Acceptance 001

```text
EXECUTION_ID=FORGE_AURA_PIPELINE_UX_DIRECTIVE_RECONCILIATION_001
DRAFT_PR=278
DELIVERY_BRANCH=codex/forge-aura-pipeline-ux-reconciliation-001
RECONCILIATION_BASE_SHA=cbf493409fc9ff7787ec8da60a436cbed42dd12b
EVIDENCE_STATUS=PENDING_CI
FINAL_SHA=NOT_AVAILABLE
```

## Evidence policy

This document records evidence only for commits created after the inherited Aura baseline. The baseline SHA is not accepted as the final reconciliation SHA.

The browser evidence must be generated by `.github/workflows/pipeline-aura-ux-reconciliation-001.yml` and remain attached to the workflow run that tests the same commit.

## Local contract evidence before remote CI

```text
NODE_SYNTAX=PASS
PIPELINE_UNIT_AND_STATIC_CONTRACTS=14/14 PASS
PIPELINE_SCOPE_GUARD=DEFINED_FROM_cbf493409fc9ff7787ec8da60a436cbed42dd12b
```

Validated contracts:

- exactly one productive primary action;
- attention layer before the directory;
- maximum three explainable priorities;
- no stale signal when Timeline is disconnected;
- next-best action requires evidence and human initiation;
- actionable complete and filtered empty states;
- follow-up defaults use existing commitment or next business day without inventing time;
- card/list semantic parity;
- keyboard dialog behavior, focus trap, Escape and focus restoration;
- visible feedback plus polite live region and busy state;
- session scrub and late-result rejection;
- authenticated/RLS authority and no cross-advisor client override;
- no automatic sends, tasks, appointments, stage changes or archive;
- no fake/demo data and no unknown-as-zero;
- responsive CSS, 44 px controls and reduced-motion rules.

## Required remote acceptance matrix

| Acceptance | Configuration | Expected evidence |
| --- | --- | --- |
| Desktop | `1440x900` | Hierarchy, one CTA, three priorities, directory, no horizontal page overflow, screenshot |
| Tablet | `834x1194` | Preserved hierarchy, usable controls, no horizontal page overflow, screenshot |
| Mobile | `390x844` | Primary action, priorities before directory, one-column reflow, screenshot |
| Zoom | `200%` at `834x1194` | Reflow without horizontal page overflow, screenshot |
| Reduced motion | Chromium `prefers-reduced-motion: reduce` | Local transitions disabled |
| Keyboard only | Tab, Enter, Escape | Primary action reachable, dialog focus trapped, focus restored |
| Card/list parity | Both views | Same semantic keys and actions |
| Filter recovery | Impossible search | Active filters shown and clear action restores records |
| Human feedback | WhatsApp draft open | Visible copy confirms no message was written or sent |
| Session safety | Destroy during pending reload | Records and adapter scrubbed; late result rejected |

## Artifact contract

Expected workflow artifact:

```text
ARTIFACT_NAME=forge-aura-pipeline-ux-reconciliation-evidence
ARTIFACT_CONTENTS=
- PIPELINE-DESKTOP-1440x900.png
- PIPELINE-TABLET-834x1194.png
- PIPELINE-MOBILE-390x844.png
- PIPELINE-ZOOM-200-REDUCED-MOTION.png
- Playwright traces on failure
```

## Current result

```text
PRIMARY_ACTION=IMPLEMENTED_AWAITING_CI
ATTENTION_LAYER=IMPLEMENTED_AWAITING_CI
EXPLAINABLE_PRIORITY=IMPLEMENTED_AWAITING_CI
NEXT_BEST_ACTION=IMPLEMENTED_AWAITING_CI
ACTIONABLE_EMPTY_STATES=IMPLEMENTED_AWAITING_CI
SMART_DEFAULTS=IMPLEMENTED_AWAITING_CI
PROGRESSIVE_DISCLOSURE=IMPLEMENTED_AWAITING_CI
HUMAN_FEEDBACK=IMPLEMENTED_AWAITING_CI
CARD_LIST_PARITY=IMPLEMENTED_AWAITING_CI
KEYBOARD=AWAITING_CI
VISIBLE_FOCUS=AWAITING_CI
SCREEN_READER=CONTRACT_PRESENT_AWAITING_CI
REDUCED_MOTION=AWAITING_CI
ZOOM_200=AWAITING_CI
MOBILE=AWAITING_CI
TABLET=AWAITING_CI
DESKTOP=AWAITING_CI
SESSION_SCRUB=IMPLEMENTED_AWAITING_CI
LATE_RESULT_REJECTION=IMPLEMENTED_AWAITING_CI
TENANT_ISOLATION_PRESERVED=CONTRACT_PRESENT_AWAITING_CI
FINAL_STATUS=PENDING_CI
```

This document must be updated with the final workflow run, tested SHA and artifact result before the reconciliation can be declared PASS.
