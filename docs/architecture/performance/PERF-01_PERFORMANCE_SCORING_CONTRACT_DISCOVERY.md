# PERF-01 — Performance Scoring Contract Discovery

```text
PERF_01_PERFORMANCE_SCORING_CONTRACT_DISCOVERY=IMPLEMENTED_ACCEPTED
PERFORMANCE_BRANCH=feature/performance-scoring-contract-foundation
BASE_TAG=activity-foundation-v1
BASE_COMMIT=de90bcbda6e96f92d6d8569dca604e5a4335a8b9
DISCOVERY_SCHEMA=performance-scoring-discovery.v1
ACTIVITY_SCORING_AUTHORITY=NO
SCORING_VALUES_INVENTED=NO
PRODUCTIVE_UI_MUTATION=NO
REMOTE_DATABASE_MUTATION=NO
```

## Objective

Inventory every scoring-related statement visible in the immutable Activity
foundation before Forge defines a Performance policy.

PERF-01 does not assign points. It classifies matches as current runtime
candidates, Performance candidates, frozen Activity references, documentation,
legacy archive, tests, evidence or automation.

## Immutable source

Performance begins from `activity-foundation-v1` at `de90bcbda6e96f92d6d8569dca604e5a4335a8b9`. The Activity tag, contracts,
remote acceptance and append-only persistence remain unchanged.

## Unresolved policy

PERF-02 must explicitly resolve scoring period, weight versioning, evaluable-day
policy, correction and reversal effects, caps, floors, goals, manager overrides,
time-zone boundaries, ranking visibility and historical recomputation.

## Prohibited conclusions

A textual match is not authority. Archived rules stay archived. PERF-01 cannot
invent point values, modify Activity, mutate Supabase or mount productive UI.

## Next

`PERF-02_PERFORMANCE_SCORING_POLICY_CONTRACT`
