# Sales Pipeline Stage Registry Decision 067G11

Status: `REGISTRY_AND_OPPORTUNITY_CONTRACT_IMPLEMENTED`. Prospect lifecycle and Sales Opportunity stage are distinct; opportunity owns stage and has a separate ID. The universal registry replaces conflicting legacy candidates without deleting them, maps known aliases, defines evidence-gated transitions, terminal outcomes, re-entry, staleness and token-derived display metadata. Product/carrier labels are excluded from Core. No stage writer exists; transition validation is pure. Next: 067G12.
