# Pipeline UI Stage Integration Decision 067G12

Status: `READ_MODEL_AND_UI_INTEGRATION_IMPLEMENTED`; production stage writer blocked. UI consumes canonical registry columns, counters, opportunity/prospect separation, stage filters/metadata, transition targets and staleness. Movement controls remain disabled with an explicit reason; preview validation never mutates local state. Drag/drop is false. Registry color token is derived metadata, not independent truth. Existing shell/navigation/tokens remain reused. Next: 067G13.
