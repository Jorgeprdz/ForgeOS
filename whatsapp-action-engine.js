/*
|--------------------------------------------------------------------------
| MODULE: whatsapp-action-engine.js
|--------------------------------------------------------------------------
|
| Acciones rápidas WhatsApp.
|
|--------------------------------------------------------------------------
*/

export function abrirWhatsApp({

    phone = '',

    message = '',

    countryCode = '52'

}) {

    /*
    |--------------------------------------------------------------------------
    | Limpiar teléfono
    |--------------------------------------------------------------------------
    */

    let cleanPhone =

        phone.replace(
            /\D/g,
            ''
        );

    /*
    |--------------------------------------------------------------------------
    | Auto agregar prefijo MX
    |--------------------------------------------------------------------------
    */

    if (
        !cleanPhone.startsWith(
            countryCode
        )
    ) {

        cleanPhone =
            countryCode
            + cleanPhone;
    }

    /*
    |--------------------------------------------------------------------------
    | URL WhatsApp
    |--------------------------------------------------------------------------
    */

    const encodedMessage =

        encodeURIComponent(
            message
        );

    const url =
`
https://wa.me/${cleanPhone}?text=${encodedMessage}
        `.trim();

    /*
    |--------------------------------------------------------------------------
    | Abrir WhatsApp
    |--------------------------------------------------------------------------
    */

    window.open(

        url,

        '_blank'
    );
}