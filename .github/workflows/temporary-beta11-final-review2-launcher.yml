name: Beta 1.1 Final Review 2 Launcher

on:
  push:
    branches:
      - chore/retire-all-legacy-forge-ui

permissions:
  contents: write

jobs:
  launch-closure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout review branch
        uses: actions/checkout@v4
        with:
          ref: chore/retire-all-legacy-forge-ui
          fetch-depth: 0

      - name: Extract and execute audited closure block
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          source = Path('.github/workflows/temporary-beta11-final-review2.yml').read_text().splitlines()
          step = next(i for i, line in enumerate(source) if 'Relocate productive quote runtime and reconcile visual acceptance' in line)
          run_line = next(i for i in range(step, len(source)) if source[i].strip() == 'run: |')
          output = []
          for line in source[run_line + 1:]:
              if line and len(line) - len(line.lstrip()) <= 6:
                  break
              output.append(line[10:] if line.startswith('          ') else line)
          if not output or not any('git mv docs/static-preview/quote-preview-live' in line for line in output):
              raise SystemExit('AUDITED_CLOSURE_BLOCK_EXTRACTION_FAILED')
          Path('/tmp/beta11-final-review2-closure.sh').write_text('\n'.join(output) + '\n')
          PY
          bash -n /tmp/beta11-final-review2-closure.sh
          bash /tmp/beta11-final-review2-closure.sh

      - name: Commit closure and remove temporary automation
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/temporary-beta11-final-review2.yml
          git rm .github/workflows/temporary-beta11-final-review2-launcher.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: close Beta 1.1 final review 2 gates"
          git push origin HEAD:chore/retire-all-legacy-forge-ui
